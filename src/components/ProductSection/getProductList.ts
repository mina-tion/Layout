import CategoryList from 'components/CategoryList';
import ProductList from 'components/ProductList';
import DontMissList from 'components/ProductList';
/* 
export const getProductList = (sectionName: string) => {
	let list:any = null;
    
    switch (sectionName) {
		case 'Categories':
			list = <CategoryList />;
            break;
		case 'Trending now':
			list = <ProductList />;
            break;
		case `Don't miss`:
			list =  <DontMissList />;
            break;
		default:
			list = <ProductList />;
	}

    return list
};
 */